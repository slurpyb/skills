# storm

Stanford STORM port: multi-perspective research then a cited Wikipedia-style article.

## API keys and env vars

None. Retrieval uses connected search MCP (for example Exa from `core`) or built-in WebSearch.
