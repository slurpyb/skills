# shopify-plugin

Search Shopify docs and validate GraphQL, Liquid, and UI extension code.

## API keys and env vars

| Variable | Needed by | For |
|---|---|---|
| `OPT_OUT_INSTRUMENTATION` | skill scripts and PostToolUse hook | Set `true` to stop usage events to `shopify.dev` |
