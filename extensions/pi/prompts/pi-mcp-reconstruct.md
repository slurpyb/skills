---
description: Reconstruct a small MCP capability as focused Pi SDK tools
argument-hint: "<server> <needed operations>"
---
Use the `reconstructing-mcp-tools` and `building-pi-tools` skills.

Reconstruct this MCP capability: $ARGUMENTS

Discover and call the live surface, select only task-critical operations, then choose runtime wrap, generated client, or native replacement. Keep OAuth/secrets external and sanitize recorded fixtures. Expose SDK `customTools`; MCP or Pi extension registration is out of scope.

Done means unit tests run offline from validated fixtures, the Pi `details` contract is stable, live smoke output matches normalized fixture behavior, and schema/fixture refresh commands are documented.
