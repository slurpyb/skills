# Deep Research Summary — REPLACE_WITH_USER_TOPIC

> **Run date:** REPLACE_WITH_YYYYMMDD
> **Workdir:** REPLACE_WITH_WORKDIR
> **Source mix:** REPLACE_WITH_SOURCE_LIST
> **Round count:** REPLACE_WITH_ROUND_COUNT
> **Citation index:** [`REPLACE_WITH_YYYYMMDD-citation-index.md`](./REPLACE_WITH_YYYYMMDD-citation-index.md)
>
> Replace every `REPLACE_WITH_*` placeholder before delivering. Citation
> tags use the form `[r{N}{letter}]` where `r{N}{letter}` matches an
> entry in the citation index.

---

## TL;DR

Replace with 4–8 sentence top-level summary. Distinguish facts from
inference. Cite at least three core claims.

---

## Architecture

What does the topic *actually look like*? Primitives, control flow,
component model, data shapes. Cite every non-trivial claim, e.g.
`(see [r2a])`.

---

## Install or Setup

Concrete steps to start using / studying / engaging with the topic. CLI
commands, accounts, prerequisites, environment notes. If the topic is
non-technical, replace with `Engagement Steps`.

---

## Surface Map

The complete inventory of public-facing primitives, APIs, interfaces,
processes, or surfaces. Bullet- or table-form is fine. Each row should
have a citation.

| Surface | Purpose | Citation |
|---------|---------|----------|
| REPLACE | REPLACE | `[r2b]` |

---

## Gotchas and Limitations

Failure modes, footguns, undocumented constraints, version skew,
deprecations. Be specific; vague gotchas hide bugs. Cite each.

- REPLACE — `[r4a]`
- REPLACE — `[r4b]`

---

## Ecosystem Alternatives

What competes, complements, or replaces the topic? Where does it sit on
the bigger map? Cite each comparison.

- **Alternative:** REPLACE — REPLACE one-line tradeoff `[r3a]`
- **Complement:** REPLACE — REPLACE one-line tradeoff `[r3b]`

---

## Use Case Mapping

For 3–5 representative use cases, how does this topic map?

| Use case | Fit | Why | Citation |
|----------|-----|-----|----------|
| REPLACE | strong / partial / poor | REPLACE | `[r3c]` |

---

## Next Step Toolchain

Concrete CLIs, libraries, vendors, processes, books, or experts the user
should reach for next. Order by next-step priority.

1. **REPLACE** — REPLACE one-line reason `[r5a]`
2. **REPLACE** — REPLACE one-line reason `[r5b]`

---

## Limitations

What this run did **not** cover, and why.

- **Uncovered axis:** REPLACE — REPLACE justification.
- **Recency caveat:** REPLACE — REPLACE.
- **Source-type gaps:** REPLACE — REPLACE.

---

## Recommendation

Distinguish *fact* / *inference* / *recommendation*. Recommendations are
the operator's call, not the source's; mark them clearly.

- **Recommendation:** REPLACE
- **Confidence:** high / medium / low — REPLACE rationale

---

## Appendix: Round Index

| Round | Phase | Facet | Manifest |
|-------|-------|-------|----------|
| 0 | scope | meta | `REPLACE_WITH_YYYYMMDD-r0-manifest.json` |
| 1 | orientation | orientation | `REPLACE_WITH_YYYYMMDD-r1-manifest.json` |
| 2 | primary_surface | core_mechanics | `REPLACE_WITH_YYYYMMDD-r2-manifest.json` |
| 3 | operational | operational | `REPLACE_WITH_YYYYMMDD-r3-manifest.json` |
| 4 | edge | failure_modes | `REPLACE_WITH_YYYYMMDD-r4-manifest.json` |
| 5 | authoritative | authoritative_sources | `REPLACE_WITH_YYYYMMDD-r5-manifest.json` |
| 6 | synthesis | meta | `REPLACE_WITH_YYYYMMDD-r6-manifest.json` |
